const path = process.cwd();
const EventUser = require(`${path}/schemas/event_users.js`);
const User = require(`${path}/schemas/users.js`);
const Event = require(`${path}/schemas/events.js`);

async function findEventsOftheUser(userId){
  
}


module.exports = {
  findEventsOftheUser
}
